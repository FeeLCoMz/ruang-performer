{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/setlists/(.*)", "destination": "/" },
    // Catch-all harus di bawah agar tidak menimpa /api
    { "source": "/(.*)", "destination": "/" }
  ]
}
